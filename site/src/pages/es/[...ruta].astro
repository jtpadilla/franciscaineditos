---
import Pagina from '../../vistas/Pagina.astro';
import { rutes } from '../../vistas/rutes';
export const getStaticPaths = rutes;
const { ruta } = Astro.params;
---
<Pagina lang="es" ruta={ruta ?? ''} {...Astro.props} />
